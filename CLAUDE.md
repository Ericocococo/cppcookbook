# CLAUDE.md

## 文档同步

- 代码或目录有新增/修改/删除时，必须同步更新对应的 README.md、STRUCTURE.md 等相关文档，不能只改代码不改文档。
- 知识目录内新增/修改知识点时三方同步：① `main.cpp` 的知识点注释组（`// 知识点 x.y：标题`）② 本目录 `README.md` 对应小节（编号与 main.cpp 对应）③ 章 README 分级大纲 details（补标题行 + 更新该目录"主题/小节"计数）。缺一视为遗漏。
- 补充内容时只加不删，不要擅自删除已有内容。修改前先看全局上下文，确保改动和其他文件一致。

## README 格式

- 每个含 .cpp 的目录的 README.md 构建章节统一为：`## 2. 构建` 下挂 `### 2.1 命令行 · MinGW（Git Bash）`、`### 2.2 命令行 · MSVC（cmd）`、
  `### 2.3 命令行 · Linux / WSL`（标题带 2.x 编号，挂在 `## 2. 构建` 下，不单独作顶层章节）。
- MSVC 包含方案 A（vcvarsall + Ninja）和方案 B（VS Generator），每个方案附自己的四步版，紧跟各自方案不堆在末尾。
- MinGW 包含方案 A（Ninja）、方案 B（MinGW Makefiles）、方案 C（Ninja Multi-Config），每个附 cmd 三步版。
- 所有内联版全部用绝对路径，无变量依赖，可直接复制粘贴。
- pybind11 项目 MinGW 不适用时，该章节保留标题并注明原因。

## Python 验证脚本

- 验证脚本（`test_*.py`）计算和打印必须分开：先赋值到变量，再单独 print。
- 不在 f-string 里直接调用函数。
- 不用 assert，打印结果给人眼看即可。

## 注释规范

- 注释只描述当前状态，不引用前面章节（不写"和 vector 一样""前面讲过""如上所述"）。
- 每个知识点在自己的位置独立解释，保证跳过前面章节也能读懂。
- 不同知识点的注释之间用空行隔开，不要挤成一大块连续注释。
- 每组知识点注释第一行加 `// 知识点：xxx` 标题，整组注释后面空一行再写代码。
- 注释要写详细，不要只写一句概括，把参数含义、为什么这样写、不这样写会怎样都写上。

## 内容编排

1. **讲解像老师讲课**：按知识点一点一点由浅入深推进，先出现的浅层用法要为后面深入讲解做铺垫。
2. **先讲后用 + 知识点唯一详解**：遇到没见过的知识点要先讲再使用；同一知识点全库只详细讲一遍，位置放在它第一次被需要的目录；后续目录不再重复展开（若该知识点是当前目录主题的必要组成，最多给一句最小回顾，可指向更早的章节）。
3. **表格**：README 写知识点时该用表格就用表格：可对比的并列项（类型/字节数/范围、字面量后缀、转义字符、初始化方式、限定符、定宽类型等）一律用表格就地总结。
4. **章 README = 该章的知识点汇总**：不做全库级超大汇总 md（太大）；每层"章"（含子目录的目录，可能是子章节、子子章节、子子子章节，任意深度）自己的 README.md 汇总本层知识点——章 README 按三级结构组织：① 知识点分级大纲（每知识目录一个 `<details>` 折叠块，两级列表列 `###` 主题与 `####` 小节，编号沿用源 README）→ ② 目录总览导航表 → ③ 知识点速查总结。汇总只覆盖本章范围。

## 代码示例

- 写示例代码时，把常用写法加上，不只演示最基本的用法。

## 代码可读性

- for 循环体必须加花括号，即使只有一行。
- 防御性检查（如 `empty()`）提前 return，不要放在三元运算符里。
- 不在 return 中使用三元运算符，拆成 if/else。
- 不做多余的 `static_cast`（如 `double / size()` 已自动浮点除法）。
- 一行一条语句，不把多条逻辑压在同一行。
- 函数体不写单行（`int foo() { return 1; }`），展开成多行带花括号。
- `auto`：简单类型显式写（`size_t dot = s.find('.')`），右侧很长时用 auto（迭代器、智能指针、Arrow 类型）。

## Git 提交说明

- subject + body 放在同一个代码块里输出，不要分成两块分别标注。
- 格式：`<type>(<scope>): <subject>`，body 空一行跟在后面。
- 只输出提交说明文本，不要执行 `git commit`，由用户手动提交。
- 不加 `Co-Authored-By` 或任何署名行。
- 提交说明只描述当前这次要提交的内容，不要把之前已提交的变更重复写进来。
